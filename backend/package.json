{
  "name": "linkvault-backend",
  "version": "1.0.0",
  "description": "LinkVault backend with SQLite + Cloudinary",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "cleanup": "node -e \"require('child_process').exec('curl http://localhost:5000/api/cleanup', (err, out) => console.log(out))\""
  },
  "dependencies": {
    "better-sqlite3": "^9.0.0",
    "cloudinary": "^2.0.3",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "multer": "^1.4.5-lts.1",
    "nanoid": "^3.3.6"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  }
}